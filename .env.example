# Local environment for A2Dev (do not commit real secrets)
# Copy to .env.local and fill in values.

# GitHub Personal Access Token (fine-grained; contents:write on target repo)
GITHUB_TOKEN=

# Optional LLM providers
OPENAI_API_KEY=
ANTHROPIC_API_KEY=

# Optional local LLM endpoint
OLLAMA_HOST=http://localhost:11434

# Optional: enable postinstall smoke test
# A2DEV_POSTINSTALL_SMOKE=true

